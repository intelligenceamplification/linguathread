import PreviewClient from "./preview-client";

export const metadata = { robots: { index: false, follow: false }, alternates: { canonical: null } };

export default function MultilingualPreviewPage() {
 return <PreviewClient />;
}
